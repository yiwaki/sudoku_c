[project]
name = "py-uv"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.2.5",
    "setuptools>=79.0.1",
]

[dependency-groups]
dev = ["ruff>=0.11.7"]
